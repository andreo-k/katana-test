import { CreateDeckResponseDto } from './create-deck-response-dto';

describe('CreateDeckResponseDto', () => {
  it('should be defined', () => {
    expect(new CreateDeckResponseDto()).toBeDefined();
  });
});
